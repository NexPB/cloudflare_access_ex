name: Retire Hex package version

on:
  workflow_dispatch:
    inputs:
      reason:
        description: Retire reason
        required: true
        default: invalid
        type: choice
        options:
          - renamed
          - deprecated
          - security
          - invalid
          - other
      message:
        description: Retire message
        required: true
        default: Version has a breaking bug
        type: string
      version:
        description: Version to retire
        required: true
        default: x.y.z
        type: string

jobs:
  retire:
    runs-on: ubuntu-latest
    env:
      MESSAGE: ${{ inputs.message }}
      REASON: ${{ inputs.reason }}
      VERSION: ${{ inputs.version }}
    steps:
      - uses: actions/checkout@v6
      - uses: erlef/setup-beam@988e02bfe678367a02564f65ca2e37726dc0268f # ratchet:erlef/setup-beam@v1.9.0
        with:
          otp-version: '28.3'
          elixir-version: '1.19.4'
      - run: echo "Attempting to retire version $VERSION"
      - run: mix hex.config api_key "$HEX_AUTH_KEY"
        env:
          HEX_AUTH_KEY: ${{ secrets.HEX_AUTH_KEY }}
      - run: mix hex.user whoami
      - run: mix hex.retire cloudflare_access_ex "$VERSION" "$REASON" --message "$MESSAGE"
